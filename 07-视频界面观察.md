# 视频界面观察

说明：本文件来自培训视频抽帧 OCR，可信度低于 PDF 文档，仅作为界面与操作路径补充参考。

## 1. 视频里观察到的页面形态

从 OCR 可以较稳定识别出以下页面或路径：

- `.../application/.../arrange?tabKey=arrange`
- `.../workflow/.../update`
- `.../database`
- `publish=false`

可推断视频演示覆盖了：

- 智能体编排页
- 工作流编辑页
- 数据库 / 知识库页
- 未发布状态下的配置页

## 2. 提示词编辑器中可能存在的结构化字段

OCR 多次识别到：

- `Role`
- `Skills`
- `Constraint`
- `Output`
- `Format`
- `Check`
- `Claim`

这说明平台界面可能支持以结构化块方式编辑 prompt，而不是单一大文本框。

可操作建议：

- 角色写清身份
- Skills 写清能力、知识源、工具
- Constraint 写限制条件
- Output 写结果目标
- Format 写输出格式
- Check 写自检规则
- Claim 写强调项 / 强约束

## 3. 视频中能看到的系统变量痕迹

OCR 中反复出现：

- `SYS_USERID`
- `SYS_USERN...`
- `TamPID` / `FamPID` / 类似字段（OCR 可信度一般）

这说明平台可能给 prompt 或流程提供系统上下文变量，可用于：

- 用户身份识别
- 租户 / 组织 / 团队信息
- 场景级参数注入

但具体字段名和可用性，仍应以当前页面为准。

## 4. 视频中能看到的工具 / 能力痕迹

OCR 有一定概率识别到：

- `Short-term memory`
- `Long-term memory`
- `Memory`
- `Planning`
- `Reflection`
- `Self-critics`
- `Chain of thoughts`
- `Subgoal decomposition`
- `Calendar`
- `calculator`
- `CodeInterpreter`
- `Search`

这说明培训视频可能展示了 Agent 组成、内建能力或工具列表界面。

注意：这些识别结果受 OCR 影响较大，是否为当前平台正式配置项，需在实际界面确认。

## 5. 视频中能看到的知识库 / 数据库界面痕迹

OCR 中比较稳定出现：

- `database`
- 导入文件提示，似乎支持 `Excel`、`CSV`
- 某些条目列表展示

推测平台支持：

- 文件型知识导入
- 数据型条目管理
- 数据源列表浏览

## 6. 视频带来的实用启发

虽然 OCR 不能完全还原页面，但有三点对后续代做很有帮助：

1. 平台不是只靠自由文本 prompt，明显有结构化配置位
2. 智能体、工作流、数据库 / 知识库、发布是分模块页面
3. 系统变量与工具列表大概率能直接在页面中引用或绑定

## 7. 后续最省事的协作方式

如果你以后想让我直接“接手” HiAgent 配置，最省事的是给我这些截图：

- 智能体编排页全图
- Prompt 编辑区全图
- 插件绑定区全图
- 工作流节点全图
- 知识库参数设置弹窗
- 发布与集成页

有了这些，我能比只看口述更快地判断问题和给出修改方案。

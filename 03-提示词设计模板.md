# 提示词设计模板（按 PDF 事实 / 最小归纳分栏）

说明：
- 只基于 PDF 内容。
- 左栏写 PDF 明确事实，右栏写最小归纳。
- 不额外补充我自己的 prompt 经验模板。
- 视频 OCR 中识别到的编辑器字段，不写入本主文档。

## 1. 提示词定义

| PDF事实 | 最小归纳 |
|---|---|
| 提示词（Prompt）是用户与生成式人工智能模型交互的指令或输入，用于引导模型生成符合预期的输出。 | PDF 将提示词定义为模型输入。 |
| 提示词可以是问题、命令、场景描述或示例，帮助模型理解任务意图。 | 提示词的表现形式不止一种。 |
| 设计优质的提示词是提升大模型性能和生成结果质量的关键。 | PDF 强调提示词质量会影响输出效果。 |
| 提示词可以通过良好的设计，引导大模型生成符合预期输出。 | PDF 将提示词视为控制输出的重要手段。 |
| 文档写到：提示词包括多类技巧，如明确任务目标、提供背景信息、设置示例和约束条件等。 | PDF 认为提示词设计包含目标、背景、示例、约束等要素。 |

## 2. 好提示词的组成

| PDF事实 | 最小归纳 |
|---|---|
| 一个好的提示词一般需要包括以下几部分内容。 | PDF 明确给出了提示词组成结构。 |
| 角色：定义大模型角色，使其输出更符合需求的答案。 | 提示词可以通过角色设定影响输出风格。 |
| 任务：定义任务，让大模型具体了解你想要完成的任务。 | 任务描述是提示词组成的一部分。 |
| 工具：智能体拥有的工具。 | 提示词可描述可用工具。 |
| 要求：对任务的具体要求，需要清晰定义应何时使用。 | 提示词可约束使用条件和任务要求。 |
| 少样本示例：通过一个回答示例，让大模型自主学习，格式化大模型的输出。 | PDF 把示例作为格式和行为引导手段。 |
| 注意事项（强调）：在不断调试过程中，智能体输出效果不及预期时，需要在“注意事项”中强调正确的做法。 | “注意事项”被用于纠偏和稳定输出。 |
| 工作流：要求大模型按照特定工作流程输出，适用于有固定流程的工作。 | Prompt 可以直接写工作流要求。 |
| 规定大模型的输出格式（markdown、json 等）。 | 输出格式是提示词的一部分。 |
| 上述所有模块不一定每次提示词都需要，可以根据需求增加或减少。 | 提示词结构是可裁剪的。 |

## 3. PDF 给出的模板骨架

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到“以下是提示词模板，写任意提示词均可以参考”。 | PDF 提供了一套通用提示词模板。 |
| 模板标题依次包括：`# 你的角色`、`# 任务`、`# 工具`、`# 要求`、`# 少样本示例`、`# 工作流`、`# 注意事项`、`# 强调`、`# 安全`。 | PDF 给出的模板结构是分段式的。 |
| 在“工具”部分，模板示例写到：`## 插件1`、`## 知识库1`，并说明“在 xxx 场景下调用 / 使用”。 | 模板中会描述不同工具或知识源的使用场景。 |
| 在“要求”部分，模板示例写到：在某些场景下必须先调用某插件。 | 模板支持写明工具调用顺序和强制约束。 |
| 在“少样本示例”部分，模板示例写到：问题、参考回答。 | 模板支持用示例问题和参考回答做引导。 |
| 在“工作流”部分，模板示例写到：完成某类任务时，按照如下流程一步步分析。 | 模板支持把流程步骤写进 prompt。 |
| 在“注意事项”部分，模板示例写到：在我问到某类问题的时候，你必须先做什么，再做什么。 | 模板支持通过注意事项写强约束步骤。 |
| 在“安全”部分，模板示例写到：除了上述知识相关的问题，你都不能回答。 | 模板支持写回答边界。 |

## 4. PDF 给出的示例提示词

| PDF事实 | 最小归纳 |
|---|---|
| 文档给出“示例提示词：高考试卷出题”。 | PDF 用具体案例示范如何写提示词。 |
| 示例中出现的模块包括：`# 你的角色`、`# 你的任务`、`# 基本要求`、`# 工作流程`、`# 特殊强调`、`# 禁止事项`、`# 示例格式`、`# 补充说明`。 | 具体案例会在通用模板基础上扩展或替换部分段落。 |
| 示例里对输出内容提出了结构规范、命题原则、难度控制、题型分值、工作流程、禁止事项等要求。 | PDF 示例体现了“角色 + 任务 + 要求 + 流程 + 边界”的组合写法。 |
| 示例中给出了明确输出格式，如【题型】【分值】【知识点】【能力目标】【题干】【答案】【解析】【评分标准】。 | PDF 用案例说明了输出格式可以被明确规定。 |

## 5. PDF 对提示词调优的说明

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到：基于上述流程以及业务实践，可以根据智能体的输出，不断调整上述提示词，以达到最佳效果。 | PDF 将提示词视为可持续调整对象。 |
| 文档提到：这里的回答虽然很完整，但由于试卷题目数量非常多，大语言模型的输出字数有限制（4096 token），因此会在最后强行中断输出。 | Prompt 设计会受到模型输出长度限制影响。 |
| 文档写到：此时我们可以使用一些特殊的提示词工程技巧 / 智能体设计，让智能体输出完整的回答。 | PDF 明确把“提示词工程技巧”与输出完整性问题关联起来。 |
| 文档在提示词配置处写到：自动优化仅能做参考，不能直接优化后使用，需要结合业务实践修改。 | PDF 对自动优化结果给出限制。 |
| 文档写到：智能体输出效果不及预期时，需要在“注意事项”中强调正确的做法，此部分对于智能体调优十分重要，保持智能体输出的稳定性。 | “注意事项”在 PDF 里被赋予了重要调优作用。 |

## 6. 提示词配置区相关功能

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到“4.2 提示词配置”。 | 平台有独立的提示词配置模块。 |
| 在提示词配置处，有以下功能：编排配置、自动优化、选择模板。 | 提示词配置区至少包含这三项功能。 |
| 编排配置：选择单 Agent / 多 Agent 模式。 | Prompt 配置与智能体编排模式有关。 |
| 自动优化：将提示词使用大模型自动优化。 | 平台支持 prompt 自动优化。 |
| 选择模板：可以将配置好的提示词模板直接导入。 | 平台支持 prompt 模板导入。 |

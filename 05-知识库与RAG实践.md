# 知识库与 RAG 实践（按 PDF 事实 / 最小归纳分栏）

说明：
- 只基于 PDF 内容。
- 左栏写 PDF 明确事实，右栏写最小归纳。
- 不额外补充通用 RAG 经验和优化建议。

## 1. 知识库配置

| PDF事实 | 最小归纳 |
|---|---|
| 我们可以将知识库配置到智能体中，这样智能体就拥有了检索知识库的功能。 | 知识库是智能体可挂载能力。 |
| 让大模型能基于知识库做问答的技术叫做检索增强生成（RAG）。 | PDF 用 RAG 描述知识库问答机制。 |
| RAG 是一种使用私有或专有数据源的信息来辅助大模型输出回答的技术。 | PDF 将知识库能力与私有数据问答关联。 |

## 2. RAG 核心流程

| PDF事实 | 最小归纳 |
|---|---|
| 核心流程：1. 用户提出问题 2. RAG 系统检索知识库相关片段 3. 将相关片段与用户问题拼接成提示词发给大模型 4. 大模型做回答。 | PDF 明确给出了知识库问答的基本链路。 |

## 3. 知识库助理搭建流程

| PDF事实 | 最小归纳 |
|---|---|
| 4.5.1 知识库助理具体搭建流程包括：文档准备与预处理、文档切割、文档存储、检索、排序。 | PDF 把知识库配置描述成一条完整流程。 |
| 文档准备与预处理的目的：数据清洗和整理，提升后续知识召回效率。 | 文档预处理被用于提升召回效率。 |
| 文档准备与预处理包括：删除无效字符和数据、统一数据格式、构造分隔符。 | 预处理至少包括这三类动作。 |
| 文档切割的目的：构建“知识分片”。 | 文档切割服务于知识分片。 |
| 文档切割的原则：尽量保障切割后的知识分片语义“独立且完整”。 | PDF 强调分片完整性。 |
| 文档写到：知识库应让模型接受“少量且必要”的知识。 | PDF 强调召回内容控制。 |
| 平台支持的分段方式：自动分割、自定义分割。 | 平台至少支持两种分段方式。 |
| 自定义分割：定义分段规则、分段长度以及预处理规则等。 | 自定义分段支持配置多个参数。 |
| 文档存储：将切分好的知识分片做好向量化，存储到向量数据库中。 | 知识库存储依赖向量化。 |
| 检索：根据用户输入，从向量数据库中获取匹配的内容。 | 检索基于用户输入和向量数据库匹配。 |
| 排序：确保最相关的片段排在前面。 | 排序用于相关性优先级控制。 |

## 4. 检索与排序

| PDF事实 | 最小归纳 |
|---|---|
| 检索方式包括：向量检索、全文检索、混合检索。 | 平台支持三类检索方式。 |
| 向量检索：向量（余弦相似度）匹配。 | PDF 明确写到向量检索依赖向量相似度。 |
| 全文检索：关键词匹配。 | PDF 明确写到全文检索依赖关键词匹配。 |
| 混合检索：先全文检索再语义排序。 | PDF 明确写到混合检索的基本顺序。 |
| 向量匹配：语义相似度排序（粗排）。 | 平台排序至少包含粗排。 |
| Reranker：将用户 query 和向量匹配结果再做一轮相似度计算（精排）。 | 平台排序至少包含精排。 |
| 文档写到：文本转换为向量是降维过程，有信息损失；Reranker 会得到更准确的结果。 | PDF 说明了粗排和精排共存的原因。 |

## 5. 知识库配置参数

| PDF事实 | 最小归纳 |
|---|---|
| 知识库、问答库和术语库的配置有类似之处。 | PDF 将三者视为同类配置体系。 |
| 点击设置，可以看到多个配置选项。 | 平台存在知识库参数设置面板。 |
| 调用方式：强制模式、Auto 模式。 | 平台支持两种查库调用方式。 |
| 强制模式：每次回答都强制智能体查找知识库。 | 强制模式意味着每次必查知识库。 |
| Auto 模式：智能体自主选择是否查找知识库。 | Auto 模式意味着由智能体自行判断。 |
| 检索方式：向量检索、全文检索、混合检索。 | 检索方式是参数项之一。 |
| 召回分段数：知识库检索多少个段落作为参考。 | 平台支持控制一次召回多少段。 |
| 相似度设置：选择相似度大于多少的段落作为参考信息。 | 平台支持阈值过滤。 |
| 文档写到：相似度设置越大，召回的段落会越少。 | 阈值大小与召回数量相关。 |
| 扩展上下文：召回段落时，自动加入前后段落补充信息。 | 平台支持片段上下文扩展。 |
| 无召回回复：当查询结果中没有可用于回答的段落时，可以选择让大模型自主回复，或者设定固定回复。 | 平台支持无命中兜底策略。 |

## 6. 知识库问答实践：AI 辅导员

| PDF事实 | 最小归纳 |
|---|---|
| 目标：开发一个 AI 辅导员，让学生咨询与学校相关事务。 | PDF 给出了一个知识库问答案例。 |
| 方法：Agentic RAG。 | PDF 明确给出方法名。 |
| 流程：创建不同分类的知识库。 | 知识会被分类型管理。 |
| 将每个知识库作为节点加入工作流。 | 知识库与工作流结合使用。 |
| 工作流使用混合检索（向量检索、关键词检索）。 | Agentic RAG 案例中使用混合检索。 |
| 仅输出分段信息。 | 工作流输出不是最终完整回答，而是分段信息。 |
| 智能体调用工作流，相当于将知识库信息加入上下文做推理，并非使用工作流直接回答问题。 | PDF 明确区分“取信息”和“最终作答”。 |
| 智能体自主选择合适的工作流获取知识库信息，当未发现时可以自动选择另外一条工作流。 | Agentic RAG 包含工作流级别的知识路由。 |
| 文档写到：建议将分段长度设置得更高，保持分段完整性。 | PDF 对该案例给出了分段完整性建议。 |
| 文档示例要求：当你查找某个工作流没有查到信息的时候，可以查询其他工作流，尝试 1 次。 | PDF 明确给出了一次补查逻辑。 |

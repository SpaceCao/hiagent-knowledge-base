# HiAgent 核心概念

## 1. 登录与权限

- 培训文档给出的平台地址：`https://agent.laiyifen.com`
- 登录方式：勾选 LDAP 登录，可使用工号登录
- 登录后仍需要联系相关管理员开通权限；文档中提到可联系“张晓宇”开通相关权限

## 2. 大语言模型（LLM）

### 是什么
大语言模型本质上是基于海量数据训练出来的文本生成模型，核心可以通俗理解为：**预测下一个字/词是什么**。

### 关键特点

- 参数量越大，通常能力越强
- 有上下文窗口限制，单次可处理文本长度有限
- 有输出 token 限制，常见上限约为 4096 / 8192 token
- 输出带随机性，但可以通过 `temperature` / `top_p` 调整
- 不同模型擅长的任务不同

### 培训材料中的模型分类

#### 非推理类模型
特点：直接回答问题，不强调先思考再输出。

PDF 原文列出的非推理类模型：
- Claude-opus-4
- ChatGPT-4o-latest
- Doubao-seed-1.6（关闭思考）

适合：
- 日常问答
- 智能体底座模型
- 需要响应更快的场景

#### 推理类模型
特点：先思考，再给答案。

PDF 原文列出的推理类模型：
- o4-mini(high)
- o3
- Deepseek-R1
- Doubao-seed-1.6（开启思考）
- QwQ-32B
- Gemini-2.5-pro

适合：
- 文案撰写
- 数据分析
- 长文生成
- 更复杂的推理型任务

### PDF 原文列出的常见大模型

| 模型名称 | 说明 |
|---------|------|
| doubao-seed-1.6 | 思考可控制 |
| doubao-1.5-pro-32k | 豆包大模型，32k 上下文 |
| deepseek-v3 | 深度求索大模型，64k 上下文 |
| Qwen | 通义千问大模型 |

## 3. 提示词（Prompt）

提示词是用户给模型的任务描述，用来约束模型的角色、任务、工具使用、输出格式和行为边界。

好的提示词通常应具备：

- 任务目标明确
- 背景信息充分
- 输入输出定义清楚
- 约束具体
- 示例充分
- 格式要求明确

## 4. 插件 / 工具

大模型只是文本预测模型，若需要：

- 获取外部信息
- 查询实时数据
- 调用内部 API
- 执行实际操作

就需要给智能体配置插件或工具。

一句话区分：

- **模型**：负责理解与生成
- **工具**：负责获取外部信息、执行动作

## 5. Embedding 模型

Embedding 是把文本映射为向量的技术，用于让系统计算“语义相似度”。

关键点：

- 不同 Embedding 模型的向量标准不同
- 只有同一套向量标准下的结果，才能可靠比较相似度
- 向量适合做相似度检索，而不是直接替代原文

用途：

- 知识库语义检索
- 相似问题匹配
- 文档召回

## 6. RAG（检索增强生成）

RAG = 先检索知识，再让大模型结合检索结果生成回答。

标准流程：

1. 用户提问
2. 系统从知识库检索相关片段
3. 将问题 + 检索片段一起喂给大模型
4. 大模型基于这些上下文回答

适合：

- 私有知识问答
- 制度 / 流程 / FAQ 问答
- 文档摘要
- 基于企业内部资料回答问题

## 7. AI Agent 是什么

培训文档中的定义要点：

AI Agent = 以大语言模型为思想引擎，并且具备：

- 计划能力
- 反思能力
- 工具调用能力
- 记忆机制

因此能完成单纯 LLM 难以完成的复杂任务。

### 一个 Agent 常见组成

- 大语言模型：理解问题、做判断、规划行为
- 工具：访问外部数据或执行动作
- 计划与反思：拆步骤、复查结果、优化输出
- 记忆：短期记忆 + 长期记忆

## 8. MCP 是什么

MCP（Model Context Protocol）是一种把大模型与外部数据源 / 工具连接起来的标准协议。

可理解为：

- 给模型接入统一标准的“外部能力接口”
- 让模型能拿到本身不知道的数据
- 让模型能触发外部系统动作

在 HiAgent 中，MCP 插件属于工具接入的重要方式之一。

## 9. 智能体质量的核心影响因素

培训文档把影响质量的因素概括为三类：

### 9.1 业务 know-how

你要先知道这个业务问题该怎么解决，包括：

- 问题可以分哪些类型
- 每类问题的处理步骤是什么
- 每一步需要什么数据与工具
- 示例问题与理想答案长什么样

### 9.2 智能体搭建 know-how

需要理解：

- 底座模型如何选
- 工具怎么配
- 工作流怎么搭
- 平台里每个模块的作用是什么

### 9.3 提示词工程

需要明确：

- 场景理解
- 输入输出
- 信息处理原则
- 结果评判标准

## 10. 一句话决策框架

如果要在 HiAgent 里做方案设计，可以先按这四问判断：

1. 问题是否依赖私有知识？——优先考虑知识库 / RAG
2. 是否需要实时数据或外部动作？——优先考虑插件 / MCP
3. 是否有固定流程？——优先考虑工作流
4. 是否任务类型差异很大？——优先考虑多智能体拆分

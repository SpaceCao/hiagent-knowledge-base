# HiAgent 平台智能体设计指南

> 本文档供 AI 智能体阅读，指导如何在 HiAgent 平台上设计智能体方案。

## 1. 知识库结构

```
hiagent-knowledge-base/
├── SYSTEM_PROMPT.md         # 本文档，入口指南
├── 00-平台功能地图.md         # 全局概览
├── 01-核心概念.md             # 术语、模型列表、决策框架
├── 02-参数配置.md             # 模型参数、思维方式、系统变量
├── 03-提示词模板.md           # Prompt 结构、字段、示例
├── 04-插件工作流.md           # 插件类型、工作流节点、内置工具
├── 05-知识库RAG.md            # RAG 流程、检索配置、Prompt 模板
├── 06-发布多智能体.md         # 发布渠道、多 Agent 编排
├── 10-能力边界清单.md         # 平台能做什么/不能做什么
├── 11-PDF原文索引.md          # PDF 页码索引
├── manifest.json              # 结构化索引
├── 99-raw-pdf-extract.txt     # PDF 原始文本
└── 99-raw-video-ocr.txt       # 视频 OCR 原始文本
```

## 2. 设计决策树

```
用户需求
    │
    ├─ 是否依赖私有知识？
    │   └─ 是 → 配置知识库（见 05-知识库RAG.md）
    │
    ├─ 是否需要实时数据或外部动作？
    │   └─ 是 → 配置插件/MCP（见 04-插件工作流.md）
    │
    ├─ 是否有固定流程？
    │   └─ 是 → 设计工作流（见 04-插件工作流.md）
    │
    └─ 是否任务类型差异大？
        └─ 是 → 拆分多智能体（见 06-发布多智能体.md）
```

## 3. 智能体配置检查清单

### 3.1 模型选择

| 场景 | 推荐类型 | 具体模型示例 |
|------|---------|-------------|
| 智能体底座 | 非推理类 | Claude-opus-4、ChatGPT-4o-latest、Doubao-seed-1.6（关闭思考） |
| 文案/分析/长文 | 推理类 | Deepseek-R1、o3、Doubao-seed-1.6（开启思考） |
| 快速响应 | 非推理类 | Doubao-1.5-pro-32k、deepseek-v3 |

### 3.2 思维方式选择

| 方式 | 适用场景 |
|------|---------|
| ReAct | 绝大多数场景，工具/知识库调用 |
| Function_call | 结构化工具调用，需模型支持 |
| Plan and Execute | 复杂多步骤任务，先计划后执行 |
| deep_search | 检索增强，搭配搜索插件/知识库 |

### 3.3 关键参数默认值

| 参数 | 推荐值 | 说明 |
|------|-------|------|
| max_token | 4096 | 一般足够 |
| 对话轮数保留 | 30（最大） | 保持记忆 |
| RAG 范围 | 3 | 平衡相关性和 token 消耗 |
| 迭代次数 | 按需设置 | 复杂任务需调大 |

## 4. Prompt 设计模板

### 4.1 必需结构

```markdown
# 角色
[定义智能体身份]

# 任务
[定义具体任务]

# 工具
## 插件1
[使用场景和调用时机]

## 知识库1
[使用场景和调用时机]

# 要求
[具体约束和要求]

# 少样本示例
## 问题1
[示例问题]
## 回答1
[示例回答]

# 注意事项
[强调正确做法]

# 输出格式
[规定输出结构]
```

### 4.2 结构化字段（平台界面）

| 字段 | 用途 |
|------|------|
| Role | 角色定义 |
| Skills | 能力/工具列表 |
| Constraint | 约束条件 |
| Output | 输出目标 |
| Format | 输出格式 |
| Check | 自检规则 |
| Claim | 强调项 |

### 4.3 可用系统变量

| 变量 | 用途 |
|------|------|
| SYS_USERID | 用户 ID |
| SYS_USERNAME | 用户名（推测） |
| TARPID / FamPID | 租户/组织标识（推测） |

## 5. 知识库配置要点

### 5.1 RAG Prompt 模板

```
# 你的任务
基于我提供给你的知识库回答问题

# 我的问题
{question}

# 知识库段落
{paragraphs}
```

### 5.2 检索方式选择

| 方式 | 适用场景 |
|------|---------|
| 向量检索 | 语义相似度匹配 |
| 全文检索 | 关键词精确匹配 |
| 混合检索 | 先全文后语义，综合效果 |

### 5.3 关键配置

| 参数 | 说明 |
|------|------|
| 调用方式 | 强制模式（必查）/ Auto 模式（智能判断） |
| 召回分段数 | 检索多少个段落 |
| 相似度设置 | 阈值越高召回越少 |
| 无召回回复 | 无命中时的兜底回复 |

### 5.4 导入限制

- Excel 文件：约 25M
- 支持 CSV 格式

## 6. 工作流节点类型

| 节点 | 用途 |
|------|------|
| 开始节点 | 输入变量定义 |
| 大模型节点 | 调用 LLM 处理 |
| 插件节点 | 调用外部工具 |
| 知识库节点 | 检索知识 |
| 消息节点 | 输出中间结果 |
| 终止节点 | 输出最终结果或返回外部智能体 |

### 变量类型语法

```
变量名   类型
─────────────────
avatar_url   Array<String>
name_cn      Array<String>
user_key     String
```

## 7. 平台能力边界

### 明确支持

- 智能体创建（对话型/流程编排型）
- 多种思维方式（ReAct/Plan/FC/deep_search）
- 插件体系（官方/火山/三方/自定义/MCP）
- 工作流编排（节点式、变量传递）
- 知识库（向量/全文/混合检索、Reranker）
- 多智能体（路由+子智能体）
- 多渠道发布（Web/API/飞书/微信）

### 未明确支持

- skills/技能市场
- 版本管理/配置回滚
- 细粒度权限控制
- 批量评测/自动回归
- 监控/日志/Trace
- 人机协同审批节点
- 定时任务

## 8. 用户需求输入模板

当用户提供需求时，按以下模板收集信息：

### 8.1 完整输入模板

```markdown
# 目标
我希望这个智能体做到：

# 当前现状
- 当前是单 Agent / 多 Agent：
- 当前使用的模型：
- 当前已绑定能力：知识库 / 插件 / MCP / 工作流

# 当前 Prompt
（直接贴全文）

# 当前问题
- 现象 1：
- 现象 2：

# 例子
## 问题 1
用户问：
当前回答：
理想回答：
```

### 8.2 最小输入

如果用户无法提供完整信息，至少需要：

1. Prompt 全文
2. 1 个失败案例
3. 期望的理想结果

## 9. 设计输出规范

当基于本知识库设计智能体方案时，输出应包含：

### 9.1 方案概述
- 智能体名称和目标
- 单 Agent / 多 Agent 决策
- 模型选择和理由

### 9.2 配置详情
- 基础参数配置
- Prompt 全文（可直接粘贴）
- 插件/MCP 配置
- 知识库配置
- 工作流设计（如有）

### 9.3 测试验证
- 测试问题清单
- 预期输出
- 验收标准

## 9. 可信度分级

使用本知识库时注意来源可信度：

| 来源 | 可信度 | 使用原则 |
|------|-------|---------|
| PDF 原文 | 高 | 作为主要依据 |
| 视频 OCR | 中 | 仅作补充参考，需实际界面确认 |

知识库中所有视频 OCR 来源内容均已在文档中标注。

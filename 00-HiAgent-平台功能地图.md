# HiAgent 平台功能地图（按 PDF 事实 / 最小归纳分栏）

说明：
- 左栏只写 **PDF 中明确出现或可直接摘录的事实**。
- 右栏只做 **最小归纳**，不补充 PDF 没写的平台能力。
- 如果某项只在视频 OCR 中出现，不写入本主文档。

## 1. 平台入口与权限

| PDF事实 | 最小归纳 |
|---|---|
| 平台地址为 `https://agent.laiyifen.com`。 | HiAgent 至少有一个网页入口。 |
| 登录时勾选 LDAP 登录，可使用工号登录。 | 平台接入了公司登录体系。 |
| 登录后需联系“张晓宇”开通相关权限。 | 平台使用上存在权限开通流程。 |

## 2. 平台主功能模块

| PDF事实 | 最小归纳 |
|---|---|
| PDF 目录里明确列出：智能体创建、智能体基础配置、提示词配置、插件、工作流、知识库配置、智能体发布、多智能体。 | 这些就是 PDF 对 HiAgent 平台重点介绍的主功能模块。 |
| 文档写到“通过 HiAgent，我们可以很轻松地通过低代码的形式搭建智能体”。 | HiAgent 被定位为低代码智能体搭建平台。 |

## 3. 智能体创建

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到“点击创建智能体”，接下来填写智能体名称和功能。 | 平台提供显式的智能体创建入口。 |
| 智能体类型：多数情况下选择“对话型智能体”；如果问题仅需要一个固定的工作流程，不需要灵活性，可以直接选择“流程编排型”。 | 平台至少支持两类智能体：对话型、流程编排型。 |

## 4. 智能体基础配置

| PDF事实 | 最小归纳 |
|---|---|
| 创建智能体之后，第一步先配置相应的大语言模型，以及对应的模型参数和智能体算法。 | 基础配置至少包括：模型、模型参数、智能体算法。 |
| 随机性/核采样：控制大模型输出的随机性。 | 平台支持控制输出随机性。 |
| 文档提醒：temperature 与 top_p 都用于控制输出多样性，不推荐同时调整。 | 平台的参数配置包含常见生成采样参数。 |
| `max_token`：模型最大输出字数，一般设置为 4096 即可。 | 平台支持控制单次输出长度。 |
| 对话轮数保留：1-30，智能体对话能记住的最大轮数。 | 平台支持控制历史对话保留轮数。 |
| RAG 范围：1-10，可将多轮对话作为上下文去检索知识库；可选择打开时携带历史问题和答案，关闭时只包含问题。 | 平台支持把历史对话带入知识库检索。 |
| 思维方式包括：ReAct、Plan and Execute、Function_call、deep_search。 | 平台支持多种智能体运行方式。 |
| ReAct：让智能体可以根据需求，自由地调用工具/知识库回答问题。 | ReAct 是面向工具/知识库调用的模式。 |
| Plan and Execute：先输出计划列表，然后逐个实行。 | 平台支持“先计划后执行”的运行方式。 |
| Function_call：将智能体判断转化为工具能识别的标准化请求（如 JSON 格式的函数名、参数）。 | 平台支持结构化工具调用模式。 |
| 如果切换后的模型不支持 FC 模式，则智能体会按 ReAct 模式运行。 | Function_call 与底座模型能力有关，平台存在回退逻辑。 |
| deep_search：对 query 自动拆解和多轮检索，建议搭配搜索插件或知识库使用。 | 平台支持面向检索场景的增强搜索模式。 |
| 迭代次数：智能体最多思考的轮数；复杂任务、多次调用工具/知识库/工作流时要设置得足够大。 | 平台支持控制多轮执行上限。 |

## 5. 提示词配置

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到“4.2 提示词配置”。 | 平台有独立的提示词配置区。 |
| 在提示词配置处，有以下功能：编排配置、自动优化、选择模板。 | 提示词配置区至少包含这三项平台功能。 |
| 编排配置：选择单 Agent / 多 Agent 模式。 | Prompt 配置与单智能体/多智能体编排有关。 |
| 自动优化：将提示词使用大模型自动优化。 | 平台内置 prompt 自动优化功能。 |
| 文档提醒：自动优化仅能做参考，不能直接优化后使用，需要结合业务实践修改。 | 平台提供自动优化，但 PDF 不建议直接照搬结果。 |
| 选择模板：可以将配置好的提示词模板直接导入。 | 平台支持提示词模板复用。 |

## 6. 插件

| PDF事实 | 最小归纳 |
|---|---|
| 插件给大模型提供了能与现实世界进行交互的能力。 | 平台用插件来扩展模型能力边界。 |
| 可以将 API 获取数据的能力打包成插件，让智能体根据用户需求随时调用。 | 平台支持把 API 封装成可调用插件。 |
| 文档列出插件分类：官方插件、火山托管插件、三方插件、自定义插件、MCP 插件。 | 这些是 PDF 中明确的插件能力分类。 |
| 官方插件示例：Browser、arxiv、SQL。 | 平台至少提供部分内置官方插件。 |
| 火山托管插件：需开通火山账号并申请 AK/SK，使用需付费。 | 平台支持接入火山托管能力。 |
| 三方插件：需在对应第三方平台注册与申请，使用需要付费。 | 平台支持接入第三方云服务插件。 |
| 自定义插件：如果有已写好的 API，可以打包成 HiAgent 插件。 | 平台支持企业自定义 API 接入。 |
| MCP 插件：平台内置部分 MCP 插件；若不满足需求，可接入自定义 MCP 插件；也可从火山引擎云市场接入云端 MCP 插件。 | MCP 是 PDF 中明确列出的正式插件类型。 |

## 7. 工作流

| PDF事实 | 最小归纳 |
|---|---|
| 工作流是一系列有序的任务和操作，用于实现特定目标或完成特定业务流程。 | 平台支持流程编排。 |
| 工作流可以包括多个步骤，每个步骤可根据需要进行分支、循环和条件判断。 | 平台工作流支持基础流程控制能力。 |
| 只要某个业务实践具有固定的工作流程，就可以设计工作流让智能体根据需要调用。 | 工作流适合固定流程型场景。 |
| 工作流中可以增加各类节点，比如大模型节点、插件节点、知识库节点、消息节点等。 | 平台工作流是节点式编排。 |
| 文档列出常见工作流结构：多工具并行/串行+大模型整理；多个大模型串行计划/反思；增加知识库/多模态/数据库/长期记忆/意图识别等。 | PDF 认为工作流可用于能力编排，不只是线性流程。 |
| 在工作流中配置大模型、知识库的方法与之前一样；大模型节点选择最好最快的模型；在适当位置添加消息节点。 | 平台允许在工作流里复用模型和知识库配置。 |
| 工作流里可以自由添加多类节点，以适配多类任务。 | 工作流具备一定通用性。 |
| 开始节点的输入就是用户提供的各类基本信息；后续节点可以引用开始节点及其他节点处理后的输出。 | 工作流支持变量输入输出传递。 |
| 大模型节点的提示词可以引用工作流里的任何变量。 | Prompt 可与工作流变量联动。 |
| 消息节点：可以输出中间结果，提醒用户目前智能体的进度。 | 工作流支持中间过程展示。 |
| 终止节点可以选择直接输出变量，还是将工作流结果返回给外部智能体处理。 | 工作流既可直接出结果，也可作为外部智能体的子流程。 |
| 搭建好工作流之后，试运行之后才能发布；试运行每个节点结束之后，都可以查看节点的输入和输出值；试运行全部通过之后，就可以发布。 | 平台支持工作流试运行与节点级调试。 |
| 文档给出“反思-高质量代码生成”案例：先由一个大模型节点输出初步设计，再由另一个大模型反思优化代码。 | PDF 明确展示了多模型串行反思型工作流。 |

## 8. 知识库配置

| PDF事实 | 最小归纳 |
|---|---|
| 可以将知识库配置到智能体中，这样智能体就拥有了检索知识库的功能。 | 知识库是智能体的可挂载能力。 |
| 文档给出知识库助理搭建流程：文档准备与预处理、文档切割、文档存储、检索、排序。 | PDF 明确把知识库配置描述成完整 RAG 流程。 |
| 文档准备与预处理包括：删除无效字符和数据、统一数据格式、构造分隔符。 | 平台使用前需要做文档清洗。 |
| 文档切割的原则：尽量保障切割后的知识分片语义“独立且完整”。 | 知识分段强调完整性。 |
| 平台支持的分段方式：自动分割、自定义分割。 | 平台支持至少两种分段模式。 |
| 自定义分割可定义分段规则、分段长度以及预处理规则等。 | 平台支持细化知识切分参数。 |
| 文档存储：将切分好的知识分片做好向量化，存储到向量数据库中。 | 知识库存储基于向量化。 |
| 检索方式：向量检索、全文检索、混合检索。 | 平台支持三类检索方式。 |
| 排序包括：向量匹配粗排、Reranker 精排。 | 平台在知识召回上支持两阶段排序。 |
| 点击设置可以看到多个配置选项：调用方式、检索方式、召回分段数、相似度设置、扩展上下文、无召回回复。 | 这些是 PDF 明确列出的知识库配置项。 |
| 调用方式包括：强制模式、Auto 模式。 | 平台支持强制查库和自主查库两种方式。 |
| 无召回回复：可以让大模型自主回复，或者设定一个固定回复。 | 平台支持无命中兜底配置。 |

## 9. 知识库问答实践（Agentic RAG）

| PDF事实 | 最小归纳 |
|---|---|
| 文档给出“AI辅导员”案例，方法是 Agentic RAG。 | PDF 明确介绍了一种知识库问答实践方法。 |
| 流程包括：创建不同分类的知识库；将每个知识库作为节点加入工作流；工作流使用混合检索；仅输出分段信息；智能体调用工作流相当于将知识库信息加入上下文做推理；当未发现时可以自动选择另外一条工作流。 | PDF 里的 Agentic RAG 核心是“知识库分路由 + 工作流取片段 + 智能体再推理”。 |
| 文档建议：将每个类型的知识库做成一个工作流，让智能体自主选择合适的知识库对应工作流回答问题。 | 不同知识库可以和不同工作流绑定。 |
| 文档示例要求：当某个工作流没有查到信息时，可以查询其他工作流，尝试 1 次。 | 平台实践中支持未命中后切换其他知识路径。 |

## 10. 发布与集成

| PDF事实 | 最小归纳 |
|---|---|
| 智能体编排右上角点击“发布与集成”，可以将智能体发布至智能体中心、飞书和微信等渠道，也可发布至 web 访问。 | 平台支持多渠道发布。 |
| 文档提醒：2.1.0 以下版本的发布渠道和 web / 后端服务 API 位置有所差异，具体参考对应版本帮助文档。 | 不同平台版本的页面位置可能不同。 |
| 发布与集成中包含 web 服务和 API。 | 平台支持网页访问和 API 集成。 |
| web 服务：是一个即时 web 网页，打开后点击预览，可以看到一个对话界面，直接与智能体进行问答。 | 平台支持把智能体发布成网页聊天入口。 |
| 后端服务 API：将智能体的问答功能做成 API，可以在其他地方调用。 | 平台支持把智能体能力 API 化。 |

## 11. 多智能体

| PDF事实 | 最小归纳 |
|---|---|
| 编排配置中，选择多 Agent 模式。 | 平台有明确的多智能体模式。 |
| 在此处可以添加任何已发布的智能体。 | 多智能体由已发布智能体组成。 |
| 第一个节点是默认必选的，用于判断用户的问题是属于哪个类型。 | 多智能体编排里存在默认路由首节点。 |
| 点击下方添加节点，可以添加已发布智能体，或者直接搭建一个智能体。 | 多智能体节点来源可以是现有智能体，也可以现场新建。 |
| 然后将问题转发给对应的智能体。 | 多智能体的基本机制是分类后转发。 |

## 12. 仅按 PDF 可得到的平台主线

| PDF事实 | 最小归纳 |
|---|---|
| 文档介绍顺序依次为：创建智能体 → 基础配置 → 提示词配置 → 插件 → 工作流 → 知识库 → 发布与集成 → 多智能体。 | 如果只按 PDF 理解平台，以上就是最自然的学习和使用主线。 |

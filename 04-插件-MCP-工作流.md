# 插件、MCP 与工作流（按 PDF 事实 / 最小归纳分栏）

说明：
- 左栏写 PDF 明确事实，右栏写最小归纳。
- 视频 OCR 中识别到的工具和变量类型，单独列入第 7、8 节，与 PDF 内容区分。

## 1. 插件

| PDF事实 | 最小归纳 |
|---|---|
| 插件给大模型提供了能与现实世界进行交互的能力。 | 平台通过插件扩展模型能力。 |
| 可以将 API 获取数据的能力打包成插件，让智能体根据用户需求随时调用。 | 平台支持将 API 封装成插件供智能体调用。 |
| 文档写到：这样 Agent 就可以获取单位内部的数据，进行数据分析、智能交互等。 | 插件被用于接入企业内部数据能力。 |

## 2. 插件分类

| PDF事实 | 最小归纳 |
|---|---|
| 文档列出：4.3.1 官方插件。 | 平台有官方插件类型。 |
| 官方插件是平台官方提供的一方插件，实际服务可随产品配套部署至本地，可直接使用，无需额外收费。 | 官方插件属于平台内置或配套提供的能力。 |
| 官方插件示例：Browser、arxiv、SQL。 | PDF 明确列出了至少 3 个官方插件示例。 |
| 文档列出：4.3.2 火山托管插件。 | 平台有火山托管插件类型。 |
| 火山托管插件是平台官方接入的火山引擎云服务，使用前需要先开通火山账号并申请 AK/SK，使用需要付费。 | 火山托管插件依赖火山云服务资源。 |
| 文档列出：4.3.3 三方插件。 | 平台有三方插件类型。 |
| 三方插件是平台官方接入的第三方云服务，使用前需要在对应三方平台进行注册与申请，使用需要付费。 | 三方插件依赖第三方平台资源。 |
| 文档列出：4.3.4 自定义插件。 | 平台有自定义插件类型。 |
| 如果有已写好的 API，可以打包成 HiAgent 插件，让智能体调用。 | 平台支持把现有 API 封装成插件。 |
| 文档列出：4.3.5 MCP 插件。 | 平台有 MCP 插件类型。 |
| 平台内置部分 MCP 插件；若平台内置 MCP 插件不满足需求，可考虑接入自定义 MCP 插件；也可以从火山引擎云市场接入云端 MCP 插件。 | MCP 在 PDF 中被明确作为正式插件接入方式。 |

## 3. 工作流定义与目标

| PDF事实 | 最小归纳 |
|---|---|
| 工作流是一系列有序的任务和操作，用于实现特定目标或完成特定业务流程。 | 平台工作流用于业务流程编排。 |
| 工作流可以包括多个步骤，每个步骤有特定任务和操作，可根据需要进行分支、循环和条件判断等。 | 工作流支持基础流程控制能力。 |
| 工作流的目的是提高工作效率、减少错误和重复劳动，同时提高业务流程的可控性和可管理性。 | PDF 将工作流定位为流程提效与流程管控能力。 |
| 只要某个业务实践具有固定的工作流程，就可以设计工作流让智能体根据需要调用。 | 工作流适用于固定流程场景。 |

## 4. 工作流节点

| PDF事实 | 最小归纳 |
|---|---|
| 工作流核心是自动化，可以通过拖拉拽的形式，将各类节点组合起来，形成自动工作的流程。 | 工作流是拖拽式节点编排。 |
| 可以增加各类节点，比如大模型节点、插件节点、知识库节点、消息节点等。 | 平台至少支持这些节点类型。 |
| 文档专门列出“4.4.1 工作流节点”。 | 节点是工作流的重要构成单元。 |
| 在工作流里可以自由添加多类节点，以适配多类任务。 | 节点支持一定程度的任务适配。 |
| 开始节点的输入就是用户提供的各类基本信息（比如问题、数据等等）。 | 开始节点承担输入入口。 |
| 后续的每个节点都可以引用开始节点及其他节点处理后的输出。 | 节点之间支持输出传递和引用。 |
| 大模型节点的提示词可以引用工作流里的任何变量，让大模型动态地处理问题及数据。 | Prompt 可和工作流变量联动。 |
| 消息节点可输出中间过程或者纯文本提醒用户当前进行到哪一步。 | 工作流支持中间过程展示。 |
| 终止节点可以根据情况选择直接输出变量，还是将工作流结果返回给外部智能体解决。 | 工作流结果既可直接输出，也可回传给外部智能体。 |

## 5. 工作流试运行与发布

| PDF事实 | 最小归纳 |
|---|---|
| 搭建好工作流之后，试运行之后才能发布。 | 发布前需要试运行。 |
| 试运行每个节点结束之后，都可以查看节点的输入和输出值。 | 平台支持节点级输入输出查看。 |
| 试运行全部通过之后，就可以发布。发布之后我们才可以在智能体中配置工作流。 | 工作流发布后才能挂到智能体中使用。 |

## 6. 文档列出的工作流结构与案例

| PDF事实 | 最小归纳 |
|---|---|
| 常见的工作流结构包括：多工具并行/串行 + 大模型整理。 | PDF 明确给出“多工具 + 大模型整理”的结构。 |
| 文档举例：AI 搜索引擎，搜索多个渠道然后梳理得到调研报告。 | PDF 给出了一个检索汇总类案例。 |
| 常见的工作流结构包括：多个大模型串行计划/反思。 | PDF 明确给出“多模型串行”的结构。 |
| 文档举例：大模型计划节点 + 大模型反思节点；长文生成智能体；类 o1 慢思考智能体。 | PDF 给出多个多模型工作流案例。 |
| 文档写到：更多还可以增加知识库、多模态、数据库、长期记忆、意图识别等。 | PDF 把这些列为工作流可增加的方向。 |
| 文档给出“反思-高质量代码生成”案例：先让一个大模型节点输出初步设计，再让另一个大模型做反思，优化代码。 | PDF 明确展示了反思型代码工作流。 |
| 文档写到：该类工作流适用于优质文案生成、报告生成、论文撰写等。 | PDF 把该案例的适用范围扩展到了文案/报告/论文。 |
| 文档提醒：部分版本存在一些按钮与案例中的示意图存在差异，具体以所使用版本为准。 | 不同版本的页面细节可能不同。 |

## 7. 平台内置工具/能力列表（视频 OCR）

说明：本节内容来自视频 OCR，可信度低于 PDF 原文，是否为平台正式配置项需在实际界面确认。

视频 OCR 识别到的工具/能力列表：

| OCR 识别字段 | 推测含义 |
|---|---|
| Short-term memory | 短期记忆 |
| Long-term memory | 长期记忆 |
| Memory | 记忆模块 |
| Planning | 计划能力 |
| Reflection | 反思能力 |
| Self-critics | 自我批评/自检 |
| Chain of thoughts | 思维链 |
| Subgoal decomposition | 子目标分解 |
| Calendar | 日历 |
| calculator | 计算器 |
| CodeInterpreter | 代码解释器 |
| Search | 搜索 |

推测：这些可能是平台内置的 Agent 能力组件或工具列表。

## 8. 工作流变量类型语法（视频 OCR）

说明：本节内容来自视频 OCR，可信度低于 PDF 原文。

视频 OCR 识别到的工作流变量声明示例：

```
output st
avatar_url   Array<String>
name_cn      Array<String>
user_key     Array<String>
```

语法推测：
- 变量类型使用 `Array<String>` 形式声明数组类型
- 支持基本类型如 `String`，以及数组类型 `Array<T>`

注意：具体语法以实际平台界面为准。

# 智能体创建与参数配置（按 PDF 事实 / 最小归纳分栏）

说明：
- 左栏写 PDF 明确事实，右栏写最小归纳。
- 视频 OCR 中识别到的系统变量，单独列入第 7 节，与 PDF 内容区分。

## 1. 智能体创建

| PDF事实 | 最小归纳 |
|---|---|
| 文档写到“点击创建智能体”，接下来填写智能体名称和功能。 | 平台存在创建智能体的入口。 |
| 智能体类型：多数情况下选择“对话型智能体”；如果问题仅需要一个固定的工作流程，不需要灵活性，可以直接选择“流程编排型”。 | 平台至少支持两种智能体类型：对话型、流程编排型。 |

## 2. 创建后首先配置的内容

| PDF事实 | 最小归纳 |
|---|---|
| 创建智能体之后，第一步先配置相应的大语言模型，以及对应的模型参数和智能体算法。 | 平台把“模型、模型参数、智能体算法”作为基础配置项。 |

## 3. 模型参数

| PDF事实 | 最小归纳 |
|---|---|
| 随机性/核采样：控制大模型输出的随机性，设置的值越高，大模型输出越随机；若设置为 0，大模型每次输出的文字都是一样的。 | 平台支持控制生成随机性。 |
| 文档提醒：temperature 是“调整概率分布的形状”，top_p 是“截断概率分布的范围”；不推荐同时调整。 | 平台参数中包含 temperature/top_p 一类采样控制。 |
| `max_token`：模型最大输出字数，限制模型每次输出的最长字符数。文档写到一般设置为 4096 即可。 | 平台支持配置单次最大输出长度。 |
| 对话轮数保留：1-30，表示智能体对话能记住的最大轮数；若超过设置值，智能体就会遗忘之前的对话。 | 平台支持配置历史对话保留轮数。 |
| 文档写到对话轮数保留“一般设置到最大值即可”。 | PDF 对该参数给出了一个文档内建议。 |

## 4. RAG 范围

| PDF事实 | 最小归纳 |
|---|---|
| RAG 范围：1-10，将多轮对话作为上下文去检索知识库。 | 平台支持将历史对话带入知识库检索。 |
| 开关：打开时携带历史对话的问题和答案，关闭则表示只包含问题。 | 平台支持控制检索时是否带入历史问答。 |
| 文档写到：数值越大，多轮对话内容的相关性越高，但消耗 token 数也更高。 | RAG 范围存在上下文收益与 token 消耗的权衡。 |
| 文档写到：一般设置为 3，如果需要尽可能用充分的背景去查询知识库，就调到最高。 | PDF 对该参数给出了文档内建议。 |

## 5. 思维方式 / 智能体算法

| PDF事实 | 最小归纳 |
|---|---|
| 思维方式包括：ReAct、Plan and Execute、Function_call、deep_search。 | 平台支持多种智能体运行模式。 |
| ReAct：让智能体可以根据需求，自由地调用工具/知识库，回答问题。 | ReAct 与工具/知识库调用有关。 |
| 文档写到：绝大多数情况下使用 ReAct 即可。 | PDF 对 ReAct 给出了倾向性建议。 |
| Plan and Execute：首先输出计划列表，然后逐个实行，需要等待较长时间才能输出回答。 | Plan and Execute 是“先计划后执行”的模式。 |
| Function_call：负责将智能体的判断转化为工具能识别的标准化请求，比如 JSON 格式的函数名、参数。 | Function_call 是结构化工具调用模式。 |
| 如果切换后的模型不支持 FC 模式，则智能体会按 ReAct 模式运行，并切换成 ReAct 模式。 | Function_call 与模型能力有关，平台存在回退逻辑。 |
| deep_search：对 query 进行自动拆解和多轮检索，建议搭配搜索插件或知识库进行使用。 | deep_search 与检索增强有关。 |
| 文档写到：建议不同时开启模型自身的长思考，以免耗时过长。 | PDF 对 deep_search 的使用给出了限制性提醒。 |

## 6. 迭代次数

| PDF事实 | 最小归纳 |
|---|---|
| 迭代次数：智能体最多思考的轮数。 | 平台支持配置多轮执行上限。 |
| 文档写到：面对复杂任务时，智能体可能需要多次调用工具/知识库。 | 迭代次数与多次调用能力相关。 |
| 文档举例：舆情分类-舆情核实（反思）-舆情分数计算，类似需要调用多次工作流的情况，迭代次数要确保设置得足够大。 | 多次工作流调用场景会受迭代次数影响。 |

## 7. 系统变量（视频 OCR）

说明：本节内容来自视频 OCR，可信度低于 PDF 原文，具体字段名和可用性以实际平台界面为准。

视频 OCR 识别到的系统变量：

| OCR 识别字段 | 推测含义 |
|---|---|
| `SYS_USERID` | 用户 ID |
| `SYS_USERN...` | 推测为 SYS_USERNAME（用户名） |
| `TARPID` / `FamPID` | 推测为租户/组织/团队相关标识 |

用途推测：
- 用户身份识别
- 租户/组织/团队信息
- 场景级参数注入

在 Prompt 或工作流中可引用这些系统变量，实现用户级个性化响应。
